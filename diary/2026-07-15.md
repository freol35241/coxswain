# 2026-07-15

## Pause: no-hardware backlog cleared, waiting on hardware

Stopping here until hardware is available. State at pause: `main` at 71fac98,
pushed, working tree clean, full workspace green (fmt, clippy -D warnings, host
tests, thumbv7em no_std gate). Schema doc at v0.8, wire `schema_version` 7.

This session took the "anything without hardware" question and, finding the MVP
and Phases 6/6b/7 already done, turned to hardening what exists and closing the
schema's open questions. What landed on main (detail and per-increment numbers in
2026-07-14.md, which holds the full 14th-15th sweep):

- Verification sweep: enforced the no-alloc control path (D-020) as a CI test;
  pseudo-fuzzed the manifest signed-blob boundary; a Monte-Carlo fault campaign
  over the closed loop; and a QEMU cross-profile harness that EXECUTES invariant
  5 (estimator/guidance/supervisor bit-identical on host and Cortex-M7), which
  also caught and fixed a 32-bit-only overflow in the blob reader.
- D-030: manifest authoring reshape, uniform nesting and one `pos` notation,
  schema_version 5 -> 6, with per-union stray-sub-table enforcement.
- D-031: sensor lever-arm compensation, GNSS-only and planar. Sim emits at the
  antenna; the EKF position and SOG/COG updates compensate. Verified against the
  closed-form R*r and omega x r bias magnitudes; the no-gyro divergence case held
  as a gate.
- D-032: settled estimator fusion as unordered inverse-variance weighting (schema
  Q1), added duplicate-in-set rejection, parked per-vessel noise params on
  system-ID.
- D-033: origin moved to a `[geometry]` section, schema_version 6 -> 7,
  completing the frame story with D-031.

Housekeeping done at pause: deleted the fully-merged `cyphal-actuator-backend`
branch (local; the remote copy can go too, it has no unique commits).

## Resume here when hardware arrives

The remaining backlog is all hardware-, data-, or trial-gated, not blocked on
design:

- First-water trial behind a manual claimant (TASKS Phase 6), then autonomy conn
  grant once the failsafe matrix has bench mileage.
- Phase 8 conn-node firmware (NUCLEO-H753ZI) and Phase 9 actuator node + vessel.
- Per-vessel noise parameters and the Fossen coefficients wait on a
  system-identification campaign; until then the constant-velocity fallback and
  best-effort coefficients carry the estimator (unchanged risk, see DECISIONS).
- Dual-antenna moving-baseline heading (schema open question 2) waits on a vessel
  that has it, plus the parked Septentrio SBF driver.
- IMU/mag drivers (Phase 6) remain codeable on the host but are deprioritized off
  the first-water path; revisit if trial data shows the 5 Hz 0183 heading source
  is insufficient.

Suggested first move on return: dry-run the commissioning flow on the actual
example-vessel hardware (author its manifest, compile/sign, boot the hosted
profile on real /dev ports) before the first-water trial.
